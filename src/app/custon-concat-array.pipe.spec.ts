import { CustonConcatArrayPipe } from './custon-concat-array.pipe';

describe('CustonConcatArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CustonConcatArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
